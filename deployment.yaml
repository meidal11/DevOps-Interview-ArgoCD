apiVersion: apps/v1
kind: Deployment
metadata:
  name: argoapp
spec:
  selector:
    matchLabels:
      app: default
  replicas: 3
  template:
    metadata:
      labels:
        app: default
    spec:
      containers:
      - image: meidal11/argodocker:1
        name: argoapp
        ports:
        - containerPort: 8080
