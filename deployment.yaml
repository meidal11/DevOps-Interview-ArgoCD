apiVersion: apps/v1
kind: Deployment
metadata:
  name: argoapp
spec:
  selector:
    matchLabels:
      app: argoapp
  replicas: 2
  template:
    metadata:
      labels:
        app: argoapp
    spec:
      containers:
      - image: meidal11/argodocker:1
        name: argoapp
        ports:
        - containerPort: 8080
